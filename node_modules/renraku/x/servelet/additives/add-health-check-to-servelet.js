export function addHealthCheckToServelet(servelet) {
    return async (request) => request.method === "get"
        ? {
            headers: { "Content-Type": "text/plain" },
            ...request.path === "/"
                ? { status: 200, body: `ok ${Date.now()}` }
                : { status: 404, body: "not found" }
        }
        : servelet(request);
}
//# sourceMappingURL=add-health-check-to-servelet.js.map