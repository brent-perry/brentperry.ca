import { generateRemote } from "./generate-remote.js";
import { makeJsonRequester } from "./make-json-requester.js";
export function generateJsonBrowserRemote({ link, shape, headers }) {
    return generateRemote({
        link,
        shape,
        requester: makeJsonRequester({
            headers,
            fetch: window.fetch,
        }),
    });
}
//# sourceMappingURL=generate-json-browser-remote.js.map