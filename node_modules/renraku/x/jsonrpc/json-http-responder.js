import { contentTypeJson } from "./content-type-json.js";
const jsonrpc = "2.0";
const errorCode = -32000;
export const makeJsonHttpResponder = ({ headers }) => ({
    resultResponse: (requestId, result) => ({
        status: 200,
        headers: {
            "Content-Type": contentTypeJson,
            ...headers,
        },
        body: JSON.stringify({
            jsonrpc,
            id: requestId,
            result,
        }),
    }),
    errorResponse: (requestId, error) => ({
        status: error.code,
        headers: {
            "Content-Type": contentTypeJson,
            ...headers,
        },
        body: JSON.stringify({
            jsonrpc,
            id: requestId,
            error: {
                code: errorCode,
                message: error.message,
            },
        }),
    }),
});
//# sourceMappingURL=json-http-responder.js.map