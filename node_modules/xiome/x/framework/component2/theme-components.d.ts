import { CSS, Constructor, LitBase } from "../component2/component-types.js";
export declare const themeComponents: <xComponents extends {
    [key: string]: Constructor<LitBase>;
}>(theme: CSS, components: xComponents) => xComponents;
