import { Api } from "renraku/x/types/api/api.js";
import { ToRemote } from "renraku/x/types/remote/to-remote.js";
export interface MockLatencyRange {
    min: number;
    max: number;
}
export declare type MockLatency = false | MockLatencyRange;
export declare function addMockLatency<xApi extends Api>({ latency, remote }: {
    latency: MockLatency;
    remote: ToRemote<xApi>;
}): ToRemote<xApi>;
