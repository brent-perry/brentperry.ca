import { AccessPayload } from "../../features/auth/types/tokens/access-payload.js";
export declare function modelDataInitializationWidget({ load, reset }: {
    load: () => Promise<void>;
    reset: () => void;
}): {
    indicateDomUsage(): void;
    accessChange(access: AccessPayload): void;
};
