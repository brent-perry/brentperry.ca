import { Stripe } from "stripe";
import { StripeComplex } from "./types/stripe-complex.js";
import { Logger } from "../../../toolbox/logger/interfaces.js";
import { StoreTables } from "../api/tables/types/store-tables.js";
import { AuthTables } from "../../auth/tables/types/auth-tables.js";
export declare function stripeWebhooks({ logger, stripeComplex, tables }: {
    logger: Logger;
    stripeComplex: StripeComplex;
    tables: StoreTables & AuthTables;
}): {
    "checkout.session.completed"(event: Stripe.Event): Promise<void>;
    "invoice.paid"(event: Stripe.Event): Promise<void>;
    "invoice.payment_failed"(event: Stripe.Event): Promise<void>;
    "customer.subscription.updated"(event: Stripe.Event): Promise<void>;
};
