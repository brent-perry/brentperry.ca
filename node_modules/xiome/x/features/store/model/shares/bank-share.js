export function bankShare({ core: { state }, stripeAccountsService, triggerBankPopup, }) {
    return {
        get access() {
            return state.access;
        },
        async getStripeAccountDetails(appId) {
            return stripeAccountsService.getConnectDetails({ appId });
        },
        async setupStripeAccount(appId) {
            await triggerBankPopup(await stripeAccountsService.generateConnectSetupLink({ appId }));
        },
    };
}
//# sourceMappingURL=bank-share.js.map