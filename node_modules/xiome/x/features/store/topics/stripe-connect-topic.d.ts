import { StripeConnectOptions } from "./types/stripe-connect-options.js";
import { StripeAccountDetails } from "./types/stripe-account-details.js";
import { MerchantAuth } from "../api/policies/types/contexts/merchant-auth.js";
export declare const stripeConnectTopic: ({ accountReturningLinks, }: StripeConnectOptions) => {
    getConnectDetails({ access, stripeLiaisonForPlatform, getTablesNamespacedForApp }: MerchantAuth, { appId }: {
        appId: string;
    }): Promise<undefined | StripeAccountDetails>;
    generateConnectSetupLink({ access, stripeLiaisonForPlatform, getTablesNamespacedForApp }: MerchantAuth, { appId }: {
        appId: string;
    }): Promise<{
        stripeAccountId: string;
        stripeAccountSetupLink: string;
    }>;
};
