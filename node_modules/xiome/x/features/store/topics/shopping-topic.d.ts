import { ShoppingOptions } from "./types/shopping-options.js";
import { CustomerAuth } from "../api/policies/types/contexts/customer-auth.js";
export declare const shoppingTopic: ({ checkoutReturningLinks }: ShoppingOptions) => {
    buySubscription({ tables, stripeLiaisonForApp }: CustomerAuth, { subscriptionPlanId }: {
        subscriptionPlanId: string;
    }): Promise<boolean>;
    updateSubscription({ tables, stripeLiaisonForApp }: CustomerAuth, { subscriptionId }: {
        subscriptionId: string;
    }): Promise<boolean>;
    endSubscription({ tables, stripeLiaisonForApp }: CustomerAuth, { subscriptionId }: {
        subscriptionId: string;
    }): Promise<boolean>;
};
