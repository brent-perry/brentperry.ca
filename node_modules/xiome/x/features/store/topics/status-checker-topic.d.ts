import { StoreStatus } from "./types/store-status.js";
import { ProspectAuth } from "../api/policies/types/contexts/prosect-auth.js";
import { SecretConfig } from "../../../assembly/backend/types/secret-config.js";
export declare const statusCheckerTopic: ({ config }: {
    config: SecretConfig;
}) => {
    getStoreStatus({ tables, access, getStripeAccount }: ProspectAuth): Promise<StoreStatus>;
};
