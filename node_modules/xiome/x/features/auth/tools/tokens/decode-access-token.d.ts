import { AccessPayload } from "../../types/tokens/access-payload.js";
import { AccessToken } from "../../types/tokens/access-token.js";
export declare const decodeAccessToken: (accessToken: AccessToken) => AccessPayload;
