import { AppDraft } from "../types/apps/app-draft.js";
import { AuthApiOptions } from "../types/auth-api-options.js";
import { AppOwnerAuth } from "../policies/types/app-owner-auth.js";
export declare const appEditTopic: (options: AuthApiOptions) => {
    updateApp({ tables, access }: AppOwnerAuth, { appId, appDraft }: {
        appId: string;
        appDraft: AppDraft;
    }): Promise<void>;
    deleteApp({ tables, access }: AppOwnerAuth, { appId }: {
        appId: string;
    }): Promise<void>;
};
