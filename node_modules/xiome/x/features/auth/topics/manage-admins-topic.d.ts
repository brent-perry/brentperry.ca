import { AuthApiOptions } from "../types/auth-api-options.js";
import { AppOwnerAuth } from "../policies/types/app-owner-auth.js";
import { AdminEmailDisplay } from "../types/manage-admins/admin-email-display.js";
export declare const manageAdminsTopic: ({ rando, config, generateNickname, }: AuthApiOptions) => {
    listAdmins(auth: AppOwnerAuth, { appId }: {
        appId: string;
    }): Promise<AdminEmailDisplay[]>;
    assignPlatformUserAsAdmin(auth: AppOwnerAuth, { appId, platformUserId }: {
        appId: string;
        platformUserId: string;
    }): Promise<void>;
    assignAdmin(auth: AppOwnerAuth, { appId, email }: {
        appId: string;
        email: string;
    }): Promise<void>;
    revokeAdmin(auth: AppOwnerAuth, { appId, userId }: {
        appId: string;
        userId: string;
    }): Promise<void>;
};
