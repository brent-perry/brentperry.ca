import { SignToken } from "redcrypto/dist/types.js";
import { Scope } from "../../types/tokens/scope.js";
import { AuthTables } from "../../tables/types/auth-tables.js";
import { PermissionsEngine } from "../../../../assembly/backend/permissions2/types/permissions-engine.js";
export declare function signAuthTokens({ scope, appId, userId, tables, origins, lifespans, permissionsEngine, signToken, generateNickname, }: {
    scope: Scope;
    appId: string;
    userId: string;
    origins: string[];
    tables: AuthTables;
    permissionsEngine: PermissionsEngine;
    lifespans: {
        access: number;
        refresh: number;
    };
    signToken: SignToken;
    generateNickname: () => string;
}): Promise<import("../../../../toolbox/concurrent.js").AwaitProps<{
    accessToken: Promise<string>;
    refreshToken: Promise<string>;
}>>;
