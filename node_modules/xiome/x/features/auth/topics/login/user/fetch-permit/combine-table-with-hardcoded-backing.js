import { dbbyMemory } from "../../../../../../toolbox/dbby/dbby-memory.js";
import { dbbyHardback } from "../../../../../../toolbox/dbby/dbby-hardback.js";
export async function combineTableWithHardcodedBacking({ hardRows, actualTable, }) {
    const hardTable = await dbbyMemory();
    await hardTable.create(...hardRows);
    return dbbyHardback({
        backTable: hardTable,
        frontTable: actualTable,
    });
}
//# sourceMappingURL=combine-table-with-hardcoded-backing.js.map