import { makePermissionsEngine } from "../../../../assembly/backend/permissions2/permissions-engine.js";
export async function fetchPermissionsDisplay({ config, access, permissionsTables, }) {
    const permissionsEngine = makePermissionsEngine({
        permissionsTables,
        isPlatform: access.appId === config.platform.appDetails.appId,
    });
    return permissionsEngine.getPermissionsDisplay();
}
//# sourceMappingURL=fetch-permissions-display.js.map