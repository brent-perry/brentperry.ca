import { AuthTables } from "../../tables/types/auth-tables.js";
import { AccessPayload } from "../../types/tokens/access-payload.js";
export declare function isUserOwnerOfApp({ appId, access, tables }: {
    appId: string;
    tables: AuthTables;
    access: AccessPayload;
}): Promise<boolean>;
