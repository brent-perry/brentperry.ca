import { AppDraft } from "../types/apps/app-draft.js";
import { AppDisplay } from "../types/apps/app-display.js";
import { AuthApiOptions } from "../types/auth-api-options.js";
import { PlatformUserAuth } from "../policies/types/platform-user-auth.js";
export declare const appTopic: ({ rando, config, }: AuthApiOptions) => {
    listApps({ tables, statsHub }: PlatformUserAuth, { ownerUserId }: {
        ownerUserId: string;
    }): Promise<AppDisplay[]>;
    registerApp({ tables }: PlatformUserAuth, { appDraft, ownerUserId }: {
        appDraft: AppDraft;
        ownerUserId: string;
    }): Promise<AppDisplay>;
};
