import { UserAuth } from "../policies/types/user-auth.js";
import { AuthApiOptions } from "../types/auth-api-options.js";
import { ProfileDraft } from "./personal/types/profile-draft.js";
export declare const personalTopic: ({ config }: AuthApiOptions) => {
    setProfile({ access, tables, checker }: UserAuth, { userId, profileDraft }: {
        userId: string;
        profileDraft: ProfileDraft;
    }): Promise<void>;
};
