import { AuthTables } from "../types/auth-tables.js";
import { SecretConfig } from "../../../../assembly/backend/types/secret-config.js";
export declare function authTablesBakery({ config, tables }: {
    tables: AuthTables;
    config: SecretConfig;
}): (appId: string) => Promise<AuthTables>;
