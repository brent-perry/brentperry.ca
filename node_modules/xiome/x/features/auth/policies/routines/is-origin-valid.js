export function isOriginValid(request, origins) {
    return !!request.headers.origin
        && origins.some(origin => origin === request.headers.origin);
}
//# sourceMappingURL=is-origin-valid.js.map