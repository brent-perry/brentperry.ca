import { User } from "../../../auth/types/user.js";
import { Question } from "../../api/types/question.js";
export declare function renderQuestion({ author, authority, question, handleDelete, handleLike, handleReport, }: {
    author: User;
    authority: boolean;
    question: Question;
    handleDelete: () => void;
    handleLike: (like: boolean) => void;
    handleReport: (report: boolean) => void;
}): import("lit-html").TemplateResult;
