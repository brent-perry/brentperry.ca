export function and(...conditions) {
    return ["and", ...conditions];
}
export function or(...conditions) {
    return ["or", ...conditions];
}
export function find(...rows) {
    return rows.length
        ? { conditions: or(...rows.map(row => ({ equal: row }))) }
        : { conditions: false };
}
//# sourceMappingURL=dbby-helpers.js.map