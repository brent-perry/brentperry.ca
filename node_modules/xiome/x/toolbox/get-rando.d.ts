import { Await } from "../types/await.js";
export declare type Rando = Await<ReturnType<typeof getRando>>;
export declare function getRando({ isNode }?: {
    isNode?: boolean;
}): Promise<{
    random: () => number;
    compare: (a: string, b: string) => boolean;
    randomId: () => string;
    randomBuffer: (bytes: number) => ArrayBuffer;
    randomSample: <T>(palette: T[]) => T;
    randomSequence: (length: number, palette: string[]) => string;
}>;
