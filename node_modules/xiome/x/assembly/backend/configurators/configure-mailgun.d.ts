import { ConfigEmailMailgun } from "../types/config-email-mailgun.js";
export declare function configureMailgun({ mailgun: { apiKey, publicKey, from, domain } }: ConfigEmailMailgun): {
    sendEmail: ({ to, body, subject }: {
        to: any;
        body: any;
        subject: any;
    }) => Promise<void>;
};
