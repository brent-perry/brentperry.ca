import { Privileges } from "../permissions-helpers.js";
import { AccessPayload } from "../../../../features/auth/types/tokens/access-payload.js";
export declare function makeAllowanceChecker<xPrivileges extends Privileges>(access: AccessPayload, privileges: xPrivileges): (privilege: keyof xPrivileges) => boolean;
