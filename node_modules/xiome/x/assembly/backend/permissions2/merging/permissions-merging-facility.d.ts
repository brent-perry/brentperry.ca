import { AnyPermissions } from "../permissions-helpers.js";
import { RoleHasPrivilegeRow } from "../../../../features/auth/tables/types/rows/role-has-privilege-row.js";
export declare function permissionsMergingFacility({ isPlatform }: {
    isPlatform: boolean;
}): {
    hardPermissions: AnyPermissions;
    mergeRoleHasPrivileges: ({ hard, soft }: {
        hard: {
            privilegeId: string;
            roleId: string;
            active: boolean;
            immutable: boolean;
        }[];
        soft: RoleHasPrivilegeRow[];
    }) => RoleHasPrivilegeRow[];
    getActivePrivilegeIds: (roleHasPrivileges: RoleHasPrivilegeRow[]) => string[];
    getHardPrivilegeDetails: (...roleIds: string[]) => {
        privilegeId: string;
        roleId: string;
        active: boolean;
        immutable: boolean;
    }[];
};
